// This is the main entry point for the 'models' module

const Note = require('./note');

// Create a Javascript module to increase portability of our models
const models = {
    Note
};

module.exports = models;